# Frontend Environment Variables  
# Copy this file to .env and fill in your actual values  
# For cloud deployment, set these as environment variables in your hosting platform  
  
# API Base URL (leave empty to use proxy in development)  
# For production, set this to your backend URL (e.g., https://api.yourdomain.com)  
VITE_API_BASE_URL=  
  
# Frontend Port (optional, defaults to 3000)  
VITE_PORT=3000  
  
# Backend API Target for Vite Proxy (optional, defaults to http://localhost:5000)  
VITE_API_TARGET=http://localhost:5000 
